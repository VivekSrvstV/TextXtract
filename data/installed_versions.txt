

Natural Language Processing (NLP) & Machine Learning

    transformers==4.27.4 – HuggingFace Transformers for LLMs and deep learning-based NLP.

    scispacy==0.5.3 – SpaCy models customized for biomedical and scientific literature.

    spacy==3.6.1 – NLP framework used for tokenization, NER, and parsing.

    nltk==3.8.1 – Natural Language Toolkit for linguistic preprocessing (stopwords, POS tagging).

    textblob==0.17.1 – Simple NLP wrapper used for sentiment, noun phrase extraction, etc.

    scikit-learn==1.2.2 – ML algorithms used in classification, clustering, vectorization.

    tensorflow==2.11.0 – Deep learning library, possibly for custom NLP models.

    torch==2.0.0 – PyTorch deep learning framework for NLP or summarization models.

    summarizer==0.0.7 – BERT extractive summarization utility.

    tokenizers==0.13.2 – HuggingFace tokenization library.

📄 PDF/Text Extraction & Parsing

    PyPDF2==3.0.1 – PDF parser for text extraction.

    pdfplumber==0.8.0 – High-level text + layout extraction from PDFs.

    pdfreader==0.1.12 – Alternative PDF parsing library.

    pdfminer.six==20221105 – Low-level PDF parsing backend (text, layout, fonts).

    PyMuPDF==1.21.1 (i.e., fitz) – Fast PDF and image extraction library.

🌐 Web Framework & Backend

    Flask==2.2.2 – Lightweight Python web framework.

    Flask-Login==0.6.3, Flask-WTF==1.2.1, Flask-SQLAlchemy==3.0.3 – Extensions for user login, forms, and database integration.

    SQLAlchemy==2.0.7 – ORM for database operations.

📊 Data Handling & Visualization

    pandas==1.5.0 – Core data analysis tool (CSV/JSON/TSV handling).

    numpy==1.23.5 – Numerical operations (matrix/vector support).

    matplotlib==3.7.0, seaborn==0.12.2, plotly==5.23.0 – Plotting libraries for visual analytics.

📦 HuggingFace / AI Integration

    huggingface-hub==0.13.3 – Integration with pretrained models and pipelines.

    spacy-transformers==1.3.4 – Connects SpaCy with Transformer models.

🧪 Scientific / Bioinformatics Specific

    biopython==1.81 – Processing biological sequence data (e.g., for ENVO/TO parsing).

    isatools==0.14.2 – For ISA-tab metadata handling (MIAPPE compliance).

🧰 Others Critical for Functionality

    bert-extractive-summarizer==0.10.1 – For extractive document summarization.

    nmslib==2.1.1 – Fast nearest neighbor search (e.g., for document similarity).

    beautifulsoup4==4.9.3, bs4==0.0.2 – HTML/XML parsing (e.g., scraping or parsing PubMed pages).
